node_modules
dist
build
coverage
pnpm-lock.yaml
data
*.md
services/ml

# Captured provider responses — verbatim, never reformatted (13_REAL_DATA_POLICY §13.7).
# Their recorded sha256 must keep matching the bytes on disk.
**/__fixtures__/real/

# Generated, and compared BYTE FOR BYTE by scripts/check-openapi.mjs. Prettier collapses
# short arrays onto one line, which JSON.stringify never does, so a prettier pass over these
# guarantees the drift gate can never pass again -- it had in fact been failing for exactly
# this reason. The generator is the only thing that may write them.
doc/openapi.json
doc/openapi-coverage.json

# Natural Earth basemap data, fetched verbatim (apps/web/public/basemap/provenance.json
# records the source). Not hand-authored, and land-50m.json alone is >1MB of coordinates —
# reformatting buys nothing and just churns the diff.
apps/web/public/basemap/

# Generated by scripts/generate_pdf.py as an intermediate step to the PDF guides — not
# hand-authored HTML, so there is no source to keep formatted.
doc/docs/*.html
